# 🚀 Xray-core 部署與管理面板 (xrm)

專為 Debian 環境設計的 Xray Manager 一鍵部署與管理工具。支援自動配置標準化 YAML、Systemd 守護進程，以及乾淨的無痕卸載。

## 🚀 一鍵安裝

請使用 `root` 權限在終端機執行以下命令：

```bash
curl -sSL https://raw.githubusercontent.com/YouKap/xrm/main/xrm.sh | sudo bash
```
